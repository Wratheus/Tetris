name: tetris
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.3.8

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
